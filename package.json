{
  "name": "src",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-fs": "^2.4.2",
    "@tauri-apps/plugin-shell": "^2.3.1",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.7.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0",
    "react-scripts": "5.0.1",
    "react-virtuoso": "^4.18.1",
    "react-window": "^1.8.10",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "dev": "npm run tauri dev",
    "start": "craco start",
    "build": "craco build",
    "tauri": "tauri",
    "wasm:build": "wasm-pack build web-wasm --target web --out-dir ../src/wasm/pkg --release",
    "web:dev": "cross-env WEB=1 craco start",
    "web:build": "cross-env WEB=1 craco build",
    "web": "npm run wasm:build && npm run web:dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "ignorePatterns": [
      "src/wasm/pkg/**"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@craco/craco": "^7.1.0",
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/cli": "^2.8.4",
    "cross-env": "^7.0.3"
  }
}
